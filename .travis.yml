sudo: false
language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"

install:
  - pip install tox-travis
  - pip install codecov
  - pip install -r requirements.txt


script:
  - make lint
  - make test-all
